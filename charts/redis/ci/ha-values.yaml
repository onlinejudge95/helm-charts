# CI test values for HA mode
architecture: sentinel
replicaCount: 3
sentinel:
  enabled: true
  replicaCount: 3
  quorum: 2
auth:
  enabled: true
  password: "test-password"
persistence:
  enabled: true
  size: 1Gi

{{- if .Values.auth.rbac.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "etcd.fullname" . }}-root-password
  labels:
    {{- include "etcd.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.auth.rbac.rootPassword | b64enc | quote }}
{{- end }}
